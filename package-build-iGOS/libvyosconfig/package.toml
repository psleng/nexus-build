[[packages]]
name = "libvyosconfig"
commit_id = "psl-master"
scm_url = "https://github.com/psleng/libvyosconfig"
build_cmd = "eval $(opam env --root=/opt/opam --set-root); dpkg-buildpackage -uc -us -tc -b"

